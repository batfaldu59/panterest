
{% extends 'layouts/base.html.twig' %}

{% block title 'Modifications' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            {{ notification }}
            <h2 class="text-center mb-3">Modifiez vos informations</h2>
            {{ form(form) }}
            <a href="{{ path('app_account') }}">Revenir à mon compte</a>
        </div>
    </div>
{% endblock %}
